// "If principle is "+p+" , time is"+t+" and rate is"+r+" .The simple interest is "
//         before 2015
// `If principle ${p}, time is ${t} and rate is ${r}. The simple interest is ${(p*t*r)/100}`
//         Current use
